apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "class-example.fullname" . }}-test-connection"
  labels:
    {{- include "class-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "class-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
