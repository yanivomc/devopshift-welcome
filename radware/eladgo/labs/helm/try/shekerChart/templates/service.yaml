apiVersion: v1
kind: Service
metadata:
  name: {{ include "shekerChart.fullname" . }}
  labels:
    {{- include "shekerChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shekerChart.selectorLabels" . | nindent 4 }}
