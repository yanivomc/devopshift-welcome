apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shekerChart.fullname" . }}-test-connection"
  labels:
    {{- include "shekerChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shekerChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
