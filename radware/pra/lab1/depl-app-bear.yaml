apiVersion: apps/v1
kind: Deployment
metadata:
  name: bear-app
spec:
  strategy:
    type: Recreate
  replicas: 3 
  selector:
    matchLabels:
      app: bear
      type: animal
  template:
    metadata:
      labels:
        app: bear
        type: animal
    spec:
      containers:
      - name: canary-bear
        image: supergiantkir/animals:bear
        ports:
        - containerPort: 80
