apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: animals-a
  annotations:
    nginx.ingress.kubernetes.io/canary: "true"
    nginx.ingress.kubernetes.io/canary-weight: "50"
    
spec:
  ingressClassName: nginx
  rules:
  - host: animals-63-35-187-141.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
             name: animals-moose 
             port:
                number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: animals-b

spec:
  ingressClassName: nginx
  rules:
  - host: animals-63-35-187-141.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
             name: animals-bear
             port:
                number: 80                         
