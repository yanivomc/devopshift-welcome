apiVersion: v1
kind: Service
metadata:
  name: {{ include "class-example.fullname" . }}
  labels:
    {{- include "class-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "class-example.selectorLabels" . | nindent 4 }}
