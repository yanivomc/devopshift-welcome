apiVersion: v1
kind: Service
metadata:
  name: app-v1
spec:
  type: ClusterIP
  ports:
    - port: 80
      protocol: TCP
      name: http
  selector:
    app: app
    version: v1

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-v1
spec:
  replicas: 2 
  selector:
    matchLabels:
      app: app
      version: v1
  template:
    metadata:
      labels:
        app: app
        version: v1
    spec:
      containers:
        - name: app
          image: "docker.io/hashicorp/http-echo"
          args:
            - -listen=:80
            - --text="app-v1"
          ports:
            - name: http
              protocol: TCP
              containerPort: 80
